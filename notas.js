

function splitDeclaration1(text){
    let declaration= text.split(" ");
    console.log(declaration);
}
splitDeclaration1("esto es una prueba"); 

function splitDeclaration2(text){
    let declaration= text.split(" ");
    console.log(declaration[0]);
    console.log(declaration[1]);
    console.log(declaration[2]);
}
splitDeclaration2(" usar espada"); 


const rooms = ["sala1", "sala2", "sala3", "sala4", "sala5", "sala6", "sala7", "sala8", "sala9"];
    let roomDescription (
        rooms[0] = ["description", "object1", "object2", "object3"]
        rooms[1] = ["description", "object1", "object2", "object3"]
        rooms[2] = ["description", "object1", "object2", "object3"]
        rooms[3] = ["description", "object1", "object2", "object3"]
        rooms[4] = ["description", "object1", "object2", "object3"]
        rooms[5] = ["description", "object1", "object2", "object3"]
        rooms[6] = ["description", "object1", "object2", "object3"]
        rooms[7] = ["description", "object1", "object2", "object3"]
        rooms[8] = ["description", "object1", "object2", "object3"]
    )









/* 

const meses = ['enero', 'febrero', 'marzo', 'abril', 'mayo'];
console.table(meses);
const numeros = [10, 20, 30, 40, 50];
numeros.forEach(function (numero) {
    console.log(numero);
})
const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo'];
meses.forEach(function (mes) {
    if (mes == 'Marzo') { // estructura de control llamada if
        console.log('Marzo si Existe');
    }
}); 


    Crear tabla con los arrays: 
let total = 0;

function agregarCarrito(precio) {
    return total += precio;
}

function calcularImpuesto(total) {
    return 1.15 * total;
}

total = agregarCarrito(200);
total = agregarCarrito(300);
total = agregarCarrito(500);
total = agregarCarrito(600);

console.log(total);

*/

